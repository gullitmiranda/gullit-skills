# Review Capsule: <short title>

## Goal

Review this diff against standards and/or spec. Do not re-implement.

## Diff

- Fixed point:
- Diff command: `git diff <fixed-point>...HEAD`
- Commits: `git log <fixed-point>..HEAD --oneline`

## Spec / plan

- Paths or issue references the Spec axis must use:
- Out of scope (do not demand):

## Standards

- Repo standards paths:
- Note: baseline smells apply only when using the `code-review` Standards axis.

## Constraints

- Read-only unless the parent explicitly asked for fixes.
- Things the reviewer must not do:

## Expected Return

Findings only, under the axis requested (Standards and/or Spec). Under 400
words per axis unless the parent specifies otherwise.

## Recommended Next Action

Run the assigned review axis and return findings.
